{
  "type": "module",
  "name": "3d_lobby",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build:netlify": "node scripts/verify-netlify-assets.mjs && esbuild netlify/app.js --bundle --outfile=netlify/bundle.js --format=esm --platform=browser",
    "watch:netlify": "esbuild netlify/app.js --bundle --outfile=netlify/bundle.js --format=esm --platform=browser --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "three": "^0.160.0"
  }
}
